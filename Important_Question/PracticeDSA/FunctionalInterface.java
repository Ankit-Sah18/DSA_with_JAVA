package Important_Question.PracticeDSA;

public interface FunctionalInterface {

    // functional interface in java is a interface which contain only one abstract method although inside the functional interface 
    // can have multiple static and default method and its also kown as Single Abstract Method (SAM) Interface..

    // Lambda excepression is a essentialy annamed method which is not implemented by its own insted it use to method defined by functional 
     // interface...

     void addDigit(int a, int b);
    
}
